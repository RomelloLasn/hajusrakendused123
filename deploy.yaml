import: 
  - recipe/laravel.php

config:
  repository: 'git@github.com:RomelloLasn/hajusrakendused123.git'
  keep_releases: 2

hosts:
  stage:
    hostname: 'tak22lasn.itmajakas.ee'
    http_user: virt118441
    remote_user: virt118441
    deploy_path: '/data01/virt118441/domeenid/www.tak22lasn.itmajakas.ee/rakendused1'

tasks:

  opcache:clear:
    - run: killall php84-cgi || true

  deploy:
    - "deploy:prepare"
    - "deploy:vendors"
    - "artisan:storage:link"
    
    - "deploy:publish"

after:
  deploy:failed: deploy:unlock

before:
  deploy:success: opcache:clear